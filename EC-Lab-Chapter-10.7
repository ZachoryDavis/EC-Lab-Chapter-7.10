//Zachory Davis EC Lab chapter 10 PC question #7

//Purpose: This program after recieving the user's full name, should display
//the user's name in MLA format. In other words, if the user were to input
// "Zachory Ryan Davis" The program should return "Davis, Zachory Ryan"

//Input-The user should input their full name in standard order, first-middle-
//last

//Output-The program should display the user's full name in MLA format

#include <cassert>
#include <iostream>
#include <string>
#include <vector>

using namespace std;

void test();

int main()
{
	test();
}

//pseudocode:

// # Take in user's full name
void takeUserName() {
	string first, middle, last;
	cout << "Please enter your first, middle and last name. (One at a time!)" << endl;
	cin >> first
		>> middle
		>> last;
}
// # Create three vectors for the user's name
//Header(s):
vector<string> firstName(string first);
vector<string> middleName(string middle);
vector<string> lastName(string last);

//**QUESTION: I'm getting stuck on how to transfer the user's input into the
//three vectors separatly
//   ## Place user's first name into the firstName vector
//   ## Place user's middle name into the middleName vector
//   ## Place user's last name into the lastName vector

// # Create a fourth vector
vector<string> fullName(string full);

//   ## Reassemble the order of the names to match MLA format
int nameTemplate() {
	int i = 0;
	while (i < full.length) {
		...first...middle...last...full[i]...
			i = i + 1;
	}
	return full[i];

// # Add a ',' after the last name and a ' ' after the first name
	cout << last << ', ' << first << ' ' << middle << endl;
}

//Asserts:
//ERROR: No opporator "!" mattches these opperands
void test() {
	assert(firstName("Zach"), middleName("Ryan"), lastName("Davis").compare"Davis, Zach Ryan");
	assert(firstName(""), middleName(""), lastName("").compare",  ");
	assert(firstName("&^%$"), middleName("7364"), lastName("D-i_s").compare"D-i_s, &^%$ 7364");

}


